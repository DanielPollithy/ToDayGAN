- checkpoint.pth.tar by Hugo Germain https://github.com/germain-hug/S2DHM/tree/master/checkpoints/robotcar

- pca.pkl contains a PCA which was trained on the overcast reference data of the robotcar rear data

- reference_descriptors.tsv are the Netvlad vectors mentioned above after vectors = normalize(PCA.transform(normalize(original_vectors))
